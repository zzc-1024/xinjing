# TODO · 待办清单

本文件只记录「要做但尚未做」的内容；所有计划与进度见 PLAN.md。

---

## 认证与令牌

- [ ] 用户自助管理入口：修改密码、查看/吊销自己的 refresh token 列表
- [ ] 第三方授权入口（granted_to=third_party）：确定第三方如何认证自己（client_credentials 或管理面板生成令牌）
- [ ] refresh token 重放检测：旧 token 被复用即判定泄露并吊销整组
- [ ] JWT 精确撤销（可选）：按 jti 作废单张 JWT（当前靠 15 分钟短 TTL 兜底）

## 插件系统（阶段 3/4）

- [ ] ABI 能力逐项定稿（能力清单开放收集，待逐项设计）：
  - 日志（已实现最小 ABI 示例，待整合进正式能力集）
  - HTTP：让 Wasm 发起外部请求（需斟酌网络出口与安全边界）
  - 长连接：SSE 和 WebSocket（必要组件，不可忽略）
  - 存储：读写对象存储（可复用 ObjectStore 接口）
  - 键值 KV：轻量缓存（可复用 cache 接口）
  - 环境变量/配置：读取函数配置（对应 function.env_vars）
  - 时间/随机数：时钟、随机数（沙箱需宿主注入）
  - 加密：哈希、签名（可复用 auth 包）
  - 度量/指标：上报指标（阶段靠后）
  - 事件/回调：通知宿主事件（取决于 ABI 设计）
  - 定时器：延迟执行（复杂，涉及调度）
- [ ] 插件挂载到中间件链
- [ ] 插件热更新
- [ ] 插件平台：上传 wasm/源码 → 构建 → 校验 → 注册表

## 云函数（阶段 5）

- [ ] 云函数管理：函数 CRUD/版本/路由 + 执行器 + 多语言 SDK

## 缓存与基础设施

- [ ] valkey 后端（引入前核查许可证；把令牌桶改原子 Lua 脚本、补固定窗口限流）
- [ ] RSA 密钥托管/轮换方案

## 管理面板与 Node

- [ ] 管理面板 + Node 工具链（前端、husky 等；注意与 Go 后端目录约定不冲突）

## 其他

- [ ] 网关真实业务端点偏少（当前只有 /me 和 /ping，后续补充函数/插件管理端点）

---
注：所有计划与进度见 PLAN.md；本文件专注「要做但尚未做」。