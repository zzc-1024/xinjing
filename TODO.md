# TODO · 遗留问题清单

本文件记录开发过程中遗留、暂缓或待后续阶段处理的事项。
每完成一项请勾选并注明日期。

---

## 认证与令牌

- [ ] **用户自助管理入口**（可穿插）
  - 修改密码
  - 查看/吊销自己的 refresh token 列表
- [ ] **第三方授权入口**（granted_to=third_party）
  - 表结构（RefreshToken.GrantedTo / Audience）与字段已铺好，但尚无签发入口
  - 需要确定第三方如何认证自己（client_credentials 或管理面板「生成第三方令牌」）
- [ ] **refresh token 重放检测**
  - RotatedFrom 字段已预留，rotation 已实现，但未做「旧 token 被复用即判定泄露并吊销全家」的逻辑
- [ ] **JWT 精确撤销（可选）**
  - jti 字段已在 Principal/JWT 预留，但未实现「按 jti 作废单张 JWT」；当前靠 15 分钟短 TTL 兜底

## 网关与限流（A 主线）

- [ ] **A2 路由注册表**
  - 当前路由散落在 cmd/server/main.go，需要抽成集中注册表（路由 → handler → 中间件 → 权限）
  - RateLimitPolicy 表已建，但策略目前是 main.go 里硬编码的，需改为从数据库读取

## 插件系统（B 主线，原阶段 3/4）

- [ ] **B1 插件系统**
  - 需引入 wazero（Apache-2.0，合规）——当前 go.mod 中不存在（git 历史曾加过但已移除）
  - Wasm 沙箱 + ABI + 能力接口 + 热更新
- [ ] **B2 插件平台**
  - 上传 wasm/源码 → 构建 → 校验 → 注册表

## 云函数（原阶段 5）

- [ ] **B3 云函数管理**
  - 函数 CRUD/版本/路由 + 执行器 + 多语言 SDK

## 缓存与基础设施

- [ ] **valkey 后端**
  - cache.Cache.Incr 接口已预留（注释明确「阶段 2 限流场景由 valkey 提供」），但 memory 实现目前返回「不支持」错误
  - 引入前需先核查 valkey 客户端依赖的许可证（红线）
  - 引入后可：把令牌桶改成原子 Lua 脚本、补固定窗口限流
- [ ] **RSA 密钥的持久化安全**
  - 私钥文件权限已用 0600；未配置 keygen 的密钥托管/轮换方案

## 管理面板与 Node（用户明确拟引入）

- [ ] **C4 管理面板 + Node 工具链**
  - 用户后续想引入 Node：管理面板前端、husky 等
  - 当前项目尚无 package.json，引入时注意与 Go 后端目录约定不冲突
  - Node 的用武之地是管理面板/前端，届时再引入；之前「密钥生成」已明确用 Go cmd/keygen 而非 Node

## 其他 / 维护

- [ ] **网关真实业务端点偏少**
  - 当前网关只有一个受保护示例 /me 和一个公开 /ping；后续业务端点（函数、插件管理）会逐步补充
- [ ] **中文配英文的范围**
  - 已处理「运行时输出到终端的错误信息」（fmt.Errorf）；代码注释、测试断言中的中文保留（不进命令行）

---
注：PLAN.md 记录「已完成」的阶段状态；本文件专注「待办/遗留」。
